version: '2'

tasks:

  test:
    cmds:
      - go test ./...

  vet:
    cmds:
      - go vet ./...

  lint:
    cmds:
      - go get golang.org/x/lint/golint
      - golint -set_exit_status ./...

  build:
    deps:
      - test
      - vet
      - lint
    cmds:
      - go build ./...
